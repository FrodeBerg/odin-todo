import "./style.css";

document.addEventListener("DOMContentLoaded", () => {
  const page = document.getElementById("page");
  page.addEventListener("click", (event) => {

    // Prompt info
    // Create either task or project 

  });
});

const tasks = [];
const projects = [];

const task = (title, x, y) => {
  return {
    x,
    y,
    title,
  };
};

// Load data on login

// Nodes

// Each node is a project or a task

// Nodes can have colours, shapes

// Tasks can be completed or not

// Projects have percentages corresponding to the amount of finished tasks

// Projects can be linked 100% = finished

// Hover over task for information

// Add logic for projects
